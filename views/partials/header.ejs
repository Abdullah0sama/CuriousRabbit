<!DOCTYPE  html>
<html class="h-100">
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
        <link href="/stylesheet/style.css" rel="stylesheet" type="text/css">
        
    </head>
    <script src="/scripts/general.js" type="text/javascript"></script>
    <body class="bg-dark h-100">
        
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">CuriousRabbit</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <% if(user) { %>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/">Home</a>
                        </li>
                    <% } %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Discover</a>
                </li>
                </ul>
                <div class="d-flex">
                    <% if(!user) { %>
                        <a class="btn btn-secondary me-2" href="/login" id="loginRedirect">Login</a>
                        <a class="btn btn-secondary" href="/register" id="registerRedirect">Register</a>
                    <% }else{ %>

                        <a href="/inbox" class="btn btn-secondary me-2">Inbox</a>

                        <a class="btn btn-dark me-2" href="/u/<%= user.username %>" >Weclome <span id="username"><%= user.username %></span></a> 

                        <form action="/logout" method="POST">
                            <button class="btn btn-danger me-2" type="submit" id="logout">Logout</button>
                        </form>
                    <% } %>
                    </div>

                </div>
            </div>
            </nav>

